export default function WorkspacePage() {
  // Workspace creator based on his permissions could can manage users delete workspace or files in it and etc that will happen on that page
  return <div>Haha</div>;
}
